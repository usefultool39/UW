# Changelog

本文件只记录当前统一版本的用户可见结果。更早的实验、Day/月度扩展和中间升级记录通过 Git 历史追溯，不再与当前产品方向并列。

## 0.6.1 — 完成度修复与剧情收束

### 产品与剧情

- 主线通关（N10 爱丽丝被带走）后新增结局画面，不再指向不存在的"第四天/第一月路线"。
- 主界面任务引导重写为 N01–N10 顺序引导；删除全部旧月度分支文案。
- 日志面板"月度路线"列改为"序章三幕"进度（日常 / 越界 / 宣判与告别）。
- 总览页目标改为三幕主线目标，删除伐木目标。
- 角色结局与场景文案统一为预捕捉世界观（删除旧名凛斗/尤里/艾琳、第一月/第二月、远征包等残留）。
- 地图与角色称谓术语下沉（卢利特村/尤吉欧/巨神树/北门），前端补丁降级为兜底。

### 稳定性

- 修复主线日闸可被绕过的软锁：daily_tick / compound_sleep / 自动跨日一律检查剧情闸，未完成当天剧情不能跨日；删除调试残留 set_day。
- 修复时间窗零容错：主线事件在当天内错过时段仍可补做，不再永久丢失。
- 修复终局后任务指引死胡同与"休息到第四天"错误文案。
- 修复弹窗打开时键盘仍可移动角色（键盘穿透）。
- 修复音频设置使用占位键名（'***'）。

### 内容清理

- 24 个旧月度路线活动标记 deprecated，不再下发到玩家活动目录。
- 北门短程巡查解锁条件（forest_anomaly_seen）接入主线（N04 前往尽头山脉时写入）。
- 图鉴移除当前切片不可达的南湖垂钓，完成度可达 100%。
- 意图回应按钮改为中文文案。
- 补齐 5 个角色立绘占位资源，消除裂图。

### 工程

- 内容校验器新增对 precapture_day_gates 的校验（事件存在性、旗标可产生性、advance_to 合法性）。
- 后端测试 275 passed；前端单测 45 passed；Playwright 4 passed（含 N01–N10 全流程）；生产构建通过。
- 存档文件名从 30town-save 改为 uw-save。

## 0.5.0 — 内部纵向切片

### 产品与剧情

- 玩家身份统一为桐人。
- 当前范围统一为卢利特村日常、巨神树、尽头山脉越界、返村宣罪、告别和爱丽丝被带走。
- 主线使用 N01–N10 十个连续节点和固定终点。
- 停止扩写旧见习记录员、长期 Day 和月度路线。

### 界面

- 首屏改为 `UNDERWORLD · 序章`。
- HUD、任务和资源名称统一为当前世界观用语。
- 删除重复常驻教学，缩小任务面板，让地图、主目标和附近互动成为主要层级。
- 存档、读档和同步集中到菜单。
- 桌面与移动端增加当前 UI smoke 覆盖。

### 素材

- 开场使用已登记的村庄关键图。
- 保留可用肖像、核心图标、村庄清晨音乐和细雨环境声。
- 不完整、缺来源或剧情错误的素材移出 runtime 候选。
- 建立可用、可试接、仅原型和返工四级审核结论。
- 当前候选统一放入各分类的 `current/` 目录并使用稳定文件名；下一轮只写入 `candidate/`。
- 地图、角色、抓捕关键图、VFX 和 SFX 的旧修订、中间文件与 fragments 已移入 `materials/archive/superseded/`。

### 工程与文档

- 仓库、前端和后端版本统一为 `0.5.0`。
- FastAPI 继续负责权威状态，scripted NPC 保持离线完整可玩。
- 开发质量门与正式发布门禁分离。
- 新增文档一致性检查，自动阻止多版本文档目录、重复计划文件、断链和版本源不一致。
- 产品、规划、交付和素材文档收束为唯一入口；旧版本文档从工作树删除并交由 Git 历史保存。
- 素材请求目标统一为 `0.5.0`；`MANIFEST.csv` 收束为当前 runtime、当前故事基线和当前候选，整理前完整台账保留在 archive。

### 面向 0.6.0 的玩法第一阶段增量

- 关系面板升级为好感、信任、紧张三维雷达，并在选择/活动结果中显示变化前后数值。
- 西侧田野新增按天气变化的每日采药、三块一次性石碑碎片收集和基础库存结算。
- 村道公告柱新增每日三选一小任务，奖励关系点或材料。
- 巨神树训练升级为五阶段逐步变速，加入三档判定、连击/断连和阶段反馈，同时保留原有结果合同。
- 新增后端回归测试覆盖每日刷新、一次性收集、奖励结算、重复限制、非法选择和旧存档兼容。

### 面向 0.6.0 的 P1 玩法增量

- 物品栏扩展为 8 格视觉槽位，加入材料/消耗品/关键道具分类和移动端抽屉。
- 新增数据驱动 `use_item` 动作：干粮恢复体力、草药汤恢复神圣力，所有条件校验失败均保持状态不变。
- 南湖旧渡接入钓鱼咬钩 QTE，支持普通鱼/稀有鱼和每日库存产出；原有 chapter_03 gate 未绕过。
- 家中炉火接入切菜节奏 + 火候控制烹饪，支持普通/完美产出和材料消耗。
- 书库小游戏升级为现象 -> 规则 -> 结论三步推理；错误选择只给本地解释，正确链条才会提交活动结算。
- 北门巡查加入出发前的 0–2 件道具组合，服务端重新验证库存并记录组合效果。
- 线索手册增加服务端权威记忆图鉴，展示主线、碎片、活动、NPC 记忆和关系里程碑。

### 0.6.0 体验收口增量

- 主线 N01-N10 到达对应地点时，互动面板优先显示关键剧情，普通日常活动不会抢走主线时间窗口。
- `/api/story/available_events` 公开权威 `day_gate`；前端休息按钮会显示“先完成：……”的具体缺口，避免界面允许休息而后端才拒绝。
- N06 开场根据 N05 的观察、共同救助或让爱丽丝主导三种选择显示短回响；正典结果、关系结算和固定终点不变。
- 烹饪、钓鱼与书库小游戏结果通过 `mini_game_result` 进入后端权威校验；服务端会拒绝普通表现伪造完美产出、普通提竿伪造稀有鱼等不一致请求，并保持状态原子不变。
- SFX 候选的技术测量已按 WAV header 对齐到真实 0.666667 秒，并增加自动一致性回归；仍未通过人耳 QA，不进入 runtime。
- 正式发布脚本改为 fail-closed：素材准备或真人盲测未完成时，`release.sh` 会非零退出，不会把“pending”误报成发布通过。

### 当前验证

- 后端测试：275 passed。
- 前端单测：45 passed。
- Production build：通过。
- Playwright：4 passed，覆盖 N01–N10、触控首次互动、桌面和移动 UI。
- 发布仍被正式素材、3 名真人盲测和 IP 权利评估阻塞。
