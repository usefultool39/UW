# Kirito 人设目录（占位 / 非当前对局角色）

## 现状

- 本目录下的 `persona.md` 等为 **叙事与产品文档预留**（例如 [docs/product/02-core-loop.md](../../docs/product/02-core-loop.md) 第一章内容表中的 NPC1）。
- **当前可玩模拟**为双智能体 **Alice + Ugo（`eugeo`）**；后端会话与前端图鉴均 **未挂载** Kirito 为可操作 agent。
- 前端 `public/kirito.svg` 可作为将来图鉴或立绘占位，**无代码引用时仅为资源预留**。

## 后续可选方向

1. **接入对局**：在 `characters/meta.json` 增加 agent、后端 `WorldState` / 初始 seed 支持第三名、UI 图鉴与 Phaser 如需头像再 `preload`。  
2. **缩小范围**：若坚持双主角，可将本目录内容 **合并进设计文档** 后删除人设文件，避免「仓库里有桐人但游戏里见不到」的误解。  
3. **保持现状**：保留本 README，协作者以本文为准。
