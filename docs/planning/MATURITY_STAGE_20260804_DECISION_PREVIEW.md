# 阶段性改造：行动前决策预览（2026-08-04）

## 审计结论

2026-08-03 的产品/架构/交付框架已经明确：Day 1–3 是当前唯一纵切片，FR-005（行动前收益/代价/锁定原因）仍是 Partial，BL-002（活动错误恢复建议）仍在 P0。当前交互面板有“结果预览”字样，但没有把数据里的 `time_cost`、`stamina_cost`、关系/记忆/flag 效果转成玩家可比较的短信息；锁定原因也只说明“为什么不可用”，没有告诉玩家下一步怎么恢复。

## 冻结方案

只改前端展示与纯函数，不改变后端状态所有权和 API DTO：

- 新增 `actionDecisionPreview.js`，从活动/剧情/NPC 回应的已有数据构造代价、收益、可用性和恢复建议。
- `FieldInteractPanel` 在每个行动按钮上显示“代价 / 收益”芯片；锁定时显示“怎么解锁”。
- 资源可负担性只做前端提示，后端仍是最终校验；危险活动显示“资源依表现结算”，不伪造固定数值。
- 为纯函数增加单测，并在 Day 1 读书流程 E2E 断言真实 `耗时 2 刻 / 体力 -5 / 信任-关系` 信息可见。
- 不新增模型调用、不改 Session、不改故事结果、不改 save schema；scripted/hybrid/agent 共用同一展示结构。

## 验收

1. 玩家打开书库互动时，在点击前能看懂读书的时间、体力代价和关系/记忆收益。
2. 资源不足、地点、时段、前置线索和每日重复等锁定原因都能给出恢复方向。
3. 后端 199+ 测试、前端单测、build、完整 E2E 通过。
4. `git diff --check` 通过，修改文件不包含运行产物或密钥。

## 验证结果

- 后端：199 passed；安全预览 DTO API 定向测试通过。
- 前端：8 项单元测试通过；production build 通过。
- Playwright：Day 1 定向流程通过；完整 10 项 E2E 通过。
- `git diff --check`：通过。

结论：WP-1B 完成。下一执行切片转向 WP-2：让 Day 1 的两项选择在 Day 2/3 产生明确、可测试的关系回响。
