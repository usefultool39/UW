# ADR-0001：采用模块化单体

- **状态**：Accepted
- **日期**：2026-08-03（补录现状）

## 背景
单人/小团队、本地单机试玩、后端规则高度共享；微服务会增加部署和调试成本。

## 决策
FastAPI 保持单进程模块化单体，按 world、story、activity、relationship、NPC runtime、memory 划分模块。

## 后果
启动和事务简单，但必须主动限制 `Session` 和大模块膨胀。出现独立部署、多人在线或单模块必须独立扩容的事实时重评。
