# 《边境回声》智能体交接文档

- **交接日期**：2026-08-05
- **项目仓库**：`https://github.com/usefultool39/UW`
- **工作分支**：`main`
- **本次交接目标**：把当前玩法成熟化改造、素材审查与返工复核、动态审查工作台、验证基线和下一步任务整理成下一个智能体可以直接接手的版本。
- **产品版本**：`0.4.0-preview.1`
- **项目阶段**：可试玩候选工作区；当前唯一产品方向是稳定 Day 1–3 纵切片。

> 本文档是交接说明；日常素材审查和优化交流以 [`../planning/MATERIALS_AND_MACRO_REVIEW_LIVE.md`](../planning/MATERIALS_AND_MACRO_REVIEW_LIVE.md) 为准。
> 本次静态快照与逐项清单：[`MATERIALS_MACRO_AUDIT_20260805.md`](MATERIALS_MACRO_AUDIT_20260805.md) / [`MATERIALS_AUDIT_20260805.csv`](MATERIALS_AUDIT_20260805.csv)。
> 素材返工交付记录：[`../planning/MATERIALS_REWORK_STATUS_20260805.md`](../planning/MATERIALS_REWORK_STATUS_20260805.md)。

---

## 1. 下一个智能体开工前必须读什么

按以下顺序阅读，不要先读 `docs/archive/` 的旧计划：

1. [`AGENTS.md`](../../AGENTS.md)
2. [`docs/README.md`](../README.md)
3. [`docs/planning/MATERIALS_AND_MACRO_REVIEW_LIVE.md`](../planning/MATERIALS_AND_MACRO_REVIEW_LIVE.md)
4. [`docs/planning/CURRENT_STATUS.md`](../planning/CURRENT_STATUS.md)
5. [`docs/planning/NEXT_PHASE.md`](../planning/NEXT_PHASE.md)
6. [`docs/product/PRODUCT_BRIEF.md`](../product/PRODUCT_BRIEF.md)
7. [`docs/architecture/SYSTEM_OVERVIEW.md`](../architecture/SYSTEM_OVERVIEW.md)
8. [`docs/delivery/DEFINITION_OF_DONE.md`](DEFINITION_OF_DONE.md)
9. [`docs/delivery/TEST_STRATEGY.md`](TEST_STRATEGY.md)
10. [`materials/00_INDEX.md`](../../materials/00_INDEX.md)
11. [`docs/planning/MATERIALS_REWORK_STATUS_20260805.md`](../planning/MATERIALS_REWORK_STATUS_20260805.md)

### 最短记忆

- **交流与素材审查**：`docs/planning/MATERIALS_AND_MACRO_REVIEW_LIVE.md`
- **下一阶段执行**：`docs/planning/NEXT_PHASE.md`
- **当前真实状态**：`docs/planning/CURRENT_STATUS.md`
- **素材登记**：`materials/REQUESTS.csv` + `materials/MANIFEST.csv`
- **发布/交接证据**：本文档

---

## 2. 当前已经完成的事情

### 2.1 玩法成熟化

当前已经完成并进入候选工作区：

- 开场关闭后保留一次三步引导：看金色指引 → 走近目标 → 做出选择。
- Day 1 开场只保留“定位第一条线索”单一主 CTA。
- 移除与右侧任务卡重复的左侧大型路线引导。
- 活动预览显示资源代价、收益类别和恢复建议。
- 书库阅读入口去重，刻印术拼接成为唯一主读书玩法入口。
- Day 1 书库选择在 Day 2 产生互斥关系回响。
- Day 3 继续呈现两条早期关系路线的专属上下文。
- reading / meal / patrol 统一进入 `activityRegistry.js` 和统一完成流程。
- 固定 NPC、hybrid、agent 共用 activity id；agent 不能绕过后端规则。
- 边界调查初始化改为同步，修复快速选择时的 reset 竞态。
- 后端继续掌握位置、时间、资源、flag、关系、记忆、剧情闸和最终奖励裁决。

### 2.2 素材审查

当前已经建立：

- 三套视觉方向：A=村庄/首屏，B=书库/记录，C=边界/异常。
- `VIS-UI-001` 核心 UI 图标审查通过，可优先接入。
- `AUD-BGM-001` BGM A/B 可作为内测素材。
- `AUD-AMB-001` ambience v002 可作为内测素材；v001 不回退。
- `NAR-VOICE-001`、`NAR-LORE-001`、`NAR-BARK-001` 作为内容源可继续适配。
- `QA-PLAY-001` 试玩套件已准备好，但还没有 3 名陌生玩家的真实记录。

已返工并复核通过：

- `VIS-KA-001` v002 关键图：真实 PNG，桌面 `2560×1440`、移动 `1440×1920`，无水印，sha256 与 MANIFEST 一致。
- `VIS-POR-001` v002 master：真实 RGBA PNG `1024×1024`，透明背景，四角 alpha=0，无棋盘格残留；256 派生图已重新生成。
- `materials/inbox/audio/audio.meta.json`：已补齐 ambience v002 条目（lufs / true_peak / sample_rate / bit_depth / runtime_file / loop_safe）。
- `materials/tools/check_materials.py` 与 `materials/MANIFEST.csv` 已随返工更新，`29 requests` 全绿。

仍确认需要退回：

- v001 关键图/肖像存在水印、旧命名/IP 近似或尺寸问题，不能继续用于正式运行时；只保留为审计对照。
- 未接入 runtime 的素材（含已通过的 v002）不能宣称已经改善真实游戏体验。

---

## 3. 当前不可破坏的边界

来自 `AGENTS.md`，下一个智能体不得违反：

- FastAPI 后端是位置、时间、资源、flag、关系、剧情闸和永久记忆的权威。
- scripted NPC 是无 API 的完整产品基线；hybrid/agent 必须可回退。
- 被拒绝的行动不得部分写入任何状态。
- `runs/`、`data/memory/`、`frontend/dist/` 是运行产物，不是配置源。
- 不 reset、覆盖或删除未提交的既有改动。
- 优先小步兼容重构，不一次性重写 `FieldSlice.vue`、Phaser scene 或 `npc_intents.py`。
- 不要因为旧档案计划而扩写无关章节；先稳定 Day 1–3。
- 所有玩家可见变化同步更新 `CHANGELOG.md`；状态变化更新 `CURRENT_STATUS.md`；下一阶段只更新 `NEXT_PHASE.md`。

---

## 4. 当前 Git / 发布事实

- 分支：`main`
- 远端：`origin git@github.com:usefultool39/UW.git`
- 本次整理已形成交接提交（本地领先 `origin/main`）；本次交接流程会将该提交推送到 `origin/main` 并核验远端。
- 版本：`0.4.0-preview.1`
- 当前不是正式 release，不要宣称已 tag 或已完成首次玩家盲测。
- 运行产物和本机服务不要提交：`runs/`、`data/memory/`、`frontend/dist/`、`frontend/test-results/` 等已经由 `.gitignore` 排除。

### 推荐提交信息

```text
chore: prepare 0.4.0 preview handoff
```

---

## 5. 接手后的第一阶段任务（按顺序）

### P0-1：素材交付可信度（已基本完成，剩余接入验收）

已由 2026-08-05 返工与二次复核完成：

- [x] 重新导出 `VIS-KA-001` v002：真实 PNG，桌面 `2560×1440`，移动 `1440×1920`。
- [x] 重新导出 `VIS-POR-001` v002 master：真实 `1024×1024 RGBA PNG`，去掉烘焙棋盘格。
- [x] 重新生成 256 缩略图，抽查 sha256 一致。
- [x] 更新 sidecar、sha256、`materials/MANIFEST.csv` 状态；v001 已标 `changes_requested`。
- [ ] 接入前补 alpha 边缘 QA（发丝/工具/书本边缘）与 24–48px UI 可辨性检查。
- [ ] 用一张真实游戏截图验收关键图/肖像/图标/音频在 UI 中的组合效果。

### P0-2：接入少量正确素材

1. UI 图标接入 HUD、任务卡、行动预览、结果面板。
2. BGM A/B 和 ambience v002 接入真实 Day 1–3 runtime。
3. ~~更新 `materials/inbox/audio/audio.meta.json`，加入 v002 条目。~~ **已完成（2026-08-05）**
4. 用一张真实游戏截图验收 A/B/C 三层风格，不要把三种风格平铺混用。

### P0-3：证明容易上手

1. 使用 `materials/inbox/research/playtest/QA-PLAY-001_playtest_kit_v001.md`。
2. 招募 3 名未参与开发的玩家，从“新游戏”开始，不提前解释玩法。
3. 回填 `QA-PLAY-001_player01.md` 到 `player03.md`。
4. 必须记录：第一次有效互动时间、第一次卡住位置、当前目标回答、行动代价、行动收益、最想知道的悬念。
5. 将结果追加到 `MATERIALS_AND_MACRO_REVIEW_LIVE.md`，再决定是否调整引导。

### P1：内容工程化

- Lore 增加 `event_id / flag / location_id / unlock_day / priority`。
- Bark 增加稳定枚举、权重、冷却、重复保护。
- 声音圣经提取为机器可读的角色/状态/语气配置。
- 统一显示名、内部 ID、历史别名，避免 Alice/Eugeo 与旧文档名称漂移。
- 固定字体包、版本、hash、许可证副本和子集化方案。

### 暂不做

- 不先扩展开放世界或新章节。
- 不先制作商店图和营销素材。
- 不先做大规模 AI corpus。
- 不先做全量地图、sprite、VFX、SFX。
- 不用 AI agent 接入替代核心可玩性和易上手问题。

---

## 6. 验证基线与执行命令

### 素材结构检查

```bash
cd materials
python3 tools/check_materials.py
```

预期：`materials check passed: 29 requests`（在未新增试玩证据前，QA request 仍可保持 requested/received 之间的实际状态，需以当前台账为准）。

### 后端

```bash
cd backend
python -m pytest -q
```

交接前记录基线：205 passed，存在 1 个 Starlette/httpx 第三方弃用警告。

### 前端单测与构建

```bash
cd frontend
npm run test:unit
npm run build
```

交接前记录基线：前端单测 12 passed，production build 通过。

### E2E

```bash
cd frontend
PYTHON_BIN=../backend/.venv/bin/python npm run test:e2e
```

必要时使用隔离端口：

```bash
E2E_BACKEND_PORT=8875 E2E_FRONTEND_PORT=3077 \
PYTHON_BIN=../backend/.venv/bin/python npm run test:e2e
```

交接前记录基线：Playwright E2E 11 passed。

### 差异和仓库卫生

```bash
git diff --check
git status --short
```

---

## 7. 当前风险清单

| 优先级 | 风险 | 处理方式 |
|---|---|---|
| P0 | 关键图/肖像 sidecar 与实际文件不一致 | 已复核一致（PNG/RGBA、尺寸、sha256）；剩余接入验收 |
| P0 | 没有陌生玩家证据 | 完成 3 人 Day 1 盲测 |
| P0 | 新素材尚未全部进入 runtime | 小批接入后做真实截图和试玩 |
| P1 | 音频已登记但未接入 runtime | 接入 OGG 后补循环、ducking、音量验证 |
| P1 | Lore/Bark 仍是自然语言标签 | 增加稳定 ID 和 adapter |
| P1 | 文档/代码存在角色旧名漂移 | 建立显示名/内部 ID 映射 |
| P1 | 项目仍带 Underworld/二创定位 | 若未来商业化，需整体原创化迁移 |
| P2 | Phaser chunk 较大 | 当前不阻塞 Day 1–3 |

---

## 8. 给下一个智能体的第一条指令

> 先阅读本交接文档、动态审查工作台、CURRENT_STATUS 和 NEXT_PHASE；然后直接执行 P0-2 的小批 runtime 接入（UI 图标 → BGM/ambience v002 → 修复后的关键图/肖像），接入后用一张真实 UI 截图验收 A/B/C 组合，再完成 P0-3 的 3 人盲测。不要扩展新系统，不要重写核心场景，不要清理或覆盖现有未提交逻辑。每完成一个小批次，先更新动态审查工作台，再运行后端测试、前端单测、build 和相关 E2E，最后把结果写回本文件或新增 dated handoff note。

---

## 9. 交接完成定义

下一个智能体只有在以下项目全部满足后，才能把本阶段标记完成：

- [ ] 能从本文档和动态工作台复原当前目标与边界。
- [ ] `git status`、远端分支和当前版本清楚。
- [ ] 关键图/肖像实际格式与 sidecar 一致。
- [ ] 新素材的运行时接入位置和回滚方式明确。
- [ ] 后端、前端单测、build、E2E 结果有日期和命令证据。
- [ ] 3 名陌生玩家 Day 1 盲测已回填，或明确记录尚未完成。
- [ ] P0/P1 待办与动态审查工作台同步。
- [ ] 未提交改动没有被无意删除或覆盖。
---

## 10. 本次交接前复验（2026-08-05）

- 素材登记与 sidecar：`materials check passed: 29 requests`。
- 素材实物二次复核：关键图 v002 为真实 PNG（桌面 `2560×1440`、移动 `1440×1920`）；肖像 v002 为真实 RGBA PNG `1024×1024`（四角 alpha=0，无棋盘格）；BGM/ambience v002 OGG 为 `48kHz/stereo`，时长 91/83/114/104 秒；sha256 与 MANIFEST 抽查一致。
- 后端：`205 passed`；1 个既有 Starlette/httpx 弃用警告。
- 前端单测：`12 passed`。
- 前端 production build：通过；仅有 Phaser chunk 大于 500 kB 的既有体积警告。
- Playwright E2E：`11 passed`，覆盖 Day 1–3、北境巡查及后续已存在的 Day 24/28/31/39/46 smoke。
- `git diff --check`：通过。
- 本次复验没有修改玩法逻辑；运行产物仍按 `.gitignore` 排除；本轮仅对齐素材审查文档与实物，并把审查证据图放入 `docs/delivery/`。

### 2026-08-05 11:40 复验重跑

交接提交前重新执行质量门并确认：后端 `205 passed`（2.1s，1 个既有弃用警告）；前端单测 `12 passed`；production build 通过（仅有既有 Phaser chunk 体积警告）；Playwright E2E `11 passed`（38s，覆盖 Day 1–3、北境巡查、Day 24/28/31/39/46 smoke）；`git diff --check` 通过。

## 新的产品方向补充（2026-08-05）

下一位接手者不要直接沿用旧的“主动时间推进”体验。新的产品规则是：

- 玩家没有独立的日期推荐/时间推进按钮。
- 普通休息不保证跨日；跨日必须由剧情事件和日结算闸决定。
- 自动 tick、NPC 日程和环境模拟继续保留，但需要可审计的跨日原因。
- 第一章以露茵村早期 Underworld 生活为基线，重点表现天职、禁忌规则、爱丽丝/尤吉欧的不同判断和从日常走向越界冒险的转折。
- 先完成 `NEXT_PHASE.md` 的 M3-WP1～WP4，再扩展素材和 AI Agent。


## 11. 日期闸门迭代收尾记录（2026-08-05）

本次交接之后继续完成了产品方向中“剧情事件驱动日期”的代码落地。历史交接段落中的 205/11 是交接前基线；以下是最新质量门结果：

- 文档提交：`0e70fa5`，建立 Underworld Alicization 第一章叙事参考基线和日期规则。
- 代码提交：`8154e7e`，实现 `day_end_gate`、`advance_policy`、日结算自动跨日、未完成剧情闸拒绝跨日和 JSONL 跨日审计。
- 后端：`207 passed`；前端单测：`12 passed`；production build：通过；Playwright E2E：`12 passed`。
- E2E 新增证明：无独立时间推进按钮；未完成 Day 1 剧情闸不能跨日；完成书库线索事件后休息自动进入 Day 2。
- 质量截图：`runs/quality_gate_desktop.png`、`runs/quality_gate_mobile.png`。
- GitHub 维护状态：`git fetch origin` 已执行；`origin/main=25a5f84`，本地 `main=8154e7e`，领先 2 个提交；最终质量门通过后应执行 `git push origin main` 并再次核验。
- 当前仍不宣称完成真实玩家盲测、正式 tag 或商业化授权；scripted 离线模式仍是主线可玩基线。
