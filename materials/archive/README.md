# 素材隔离区

这里只保存仍有实际回滚或来源价值、但不能进入当前 runtime 的素材：

- `unreviewed/`：信息或动作不完整，等待重新审核。
- `rejected/`：内容方向错误，保留作为反例。
- 其他明确标注的技术中间包：只在仍被当前工具依赖时保留。

普通历史修订、旧指南和重复中间稿不再长期堆放，统一通过 Git 历史恢复。任何 archive 文件都不能被视为当前可用素材。
