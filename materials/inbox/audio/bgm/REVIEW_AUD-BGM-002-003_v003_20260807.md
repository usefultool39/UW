# AUD-BGM-002/003 v003 返工评审

- request_id: `AUD-BGM-002`, `AUD-BGM-003`
- reviewed_version: `v003`
- decision: `changes_requested`
- next_version: `v004`
- priority: `P1 blocker`
- runtime_status: prohibited

## 问题

- v003 报告的时长、48kHz/24-bit、LUFS 和 peak 接近目标，但 gate 尚未读取这些证据，不能标为技术通过。
- master 使用 `<stem>.wav`；active contract 使用 `<stem>_48k24b.wav`。
- metadata 交付为 `audio.meta_v003.fragment.json`，而合同入口和 schema 不一致；measurement 为带 `tracks` 的对象，gate 要求逐文件数组及 `file`、`integrated_loudness_lufs`、`peak_dbfs`。
- sidecar/manifest fragment 放在 `materials/inbox/audio/` 根目录，不在请求的 `audio/bgm/` 交付目录，readiness 无法识别版本化 sidecar。
- 尚未完成人耳循环、旋律原创性、前景重复和对话 ducking 验收。

## v004 修改要求

- 可以 v003 音源作为候选源，但必须非破坏性导出 v004；若人耳检查发现循环或内容问题则重新制作。
- 按 active v004 合同输出 master/OGG、metadata 和 measurements；sidecar 与 manifest fragment 放入 `materials/inbox/audio/bgm/`。
- metadata 每个 stem 包含 duration、sample_rate_hz、bit_depth、channels、loop_safe、loop_start_sample、loop_end_sample。
- measurement 每个 WAV 一项，包含 file、integrated_loudness_lufs、peak_dbfs；所有 SHA-256 与实物一致。

## 验收标准

- 两组四个 stem 无 runtime-spec 问题，OGG 页结构有效，时长/LUFS/peak/loop points 符合合同。
- 人耳连续播放两轮无可闻跳点、削波、长静音或过短前景重复；BGM-002 与 BGM-003 情绪可区分且不含可识别版权旋律。
- 游戏内完成 loop、ducking、静音和加载失败回退 QA 后才允许接入。
