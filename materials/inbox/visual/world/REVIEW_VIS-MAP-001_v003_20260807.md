# VIS-MAP-001 v003 返工评审

- request_id: `VIS-MAP-001`
- reviewed_version: `v003`
- decision: `changes_requested`
- next_version: `v004`
- priority: `P1 blocker`
- runtime_status: prohibited

## 问题

- 人工抽查显示 master 仍是程序化调试图：大面积黑色网格块、矩形道路、圆形光斑和几何建筑缺少可信村庄语义，不能作为面向大众的正式 2D RPG 地图。
- `terrain` 与 `water` 合并为 `terrain_water`，不满足独立水层合同；`occluders` 命名也与合同 `occlusion` 不一致。
- `VIS-MAP-001_map_v003.json` 缺顶层 `runtime_size`，`layers` 使用数组而不是按层名索引的 source 映射。
- collision/walkable 在 `masks` 下，interaction 只内嵌在 metadata；当前 gate 需要 `data.collision`、`data.walkable`、`data.interaction` 三个可解析路径。
- `tiles_atlas_v003.png`、`props_atlas_v003.png` 及 `_v003_work` 中同名文件缺 request_id 前缀，触发未登记文件错误。

## v004 修改要求

- 重新制作可读、明快、有真实卢利特村空间语义的原创地图层；禁止以调试网格、纯几何块和大面积黑洞作为正式美术。
- 独立输出 terrain、water、roads、buildings、vegetation、occlusion、foreground、lighting、weather，全部 3024x1792；需要 alpha 的层必须同时含可见与透明像素。
- 输出 request-prefixed tile/prop atlas 和独立 collision、walkable、interaction 数据。
- metadata 必须符合 `materials/runtime_asset_requirements.json`：顶层 `runtime_size`，`layers.<name>.source`，`data.<name>` 路径。
- 工作脚本和测试文件不要放在会被正式 inbox 扫描的位置；不得删除或覆盖 v003。

## 验收标准

- `check_materials.py` 不再因本请求产生未登记文件错误。
- v004 map 在 `check_runtime_asset_specs.py --require-complete` 中无问题。
- 人工审查能在无文字情况下识别教会/书库、广场、家、巨神树路线、北门和尽头山脉路线；道路连续，遮挡与前景可用于 Phaser 深度排序。
- 通过桌面与 390x844 游戏内移动、碰撞、遮挡和 UI 遮挡验收后，才允许升级状态。
