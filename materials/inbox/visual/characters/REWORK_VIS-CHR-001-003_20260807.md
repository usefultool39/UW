# Rework request: VIS-CHR-001 to VIS-CHR-003

- request_ids: VIS-CHR-001, VIS-CHR-002, VIS-CHR-003
- status: changes_requested
- expected_version: v003; do not overwrite or delete v002
- delivery_dir: materials/inbox/visual/characters
- priority: P1, first-phase runtime blocker
- reviewed_at: 2026-08-07
- runtime_status: prohibited until the full acceptance chain passes

## Findings

- The reviewed `VIS-CHR-001_kirito_down_walk_v002.png` is 1024x1024, 24-bit RGB, and contains one pose. Its checkerboard is baked into the pixels; there is no transparency.
- The remaining direction/action files follow the same single-cutout delivery pattern. File names do not prove that animation frames exist.
- The set has no frame JSON, frame rate, pivot/foot anchor, hitbox, scale proof, sidecar, rights record, or MANIFEST/hash registration.

## Required replacement

1. Deliver real transparent RGBA sprite sheets for Kirito, Alice, and Eugeo in the same original visual language and child-age proportions.
2. Use a consistent 64x96 or 96x128 frame cell across all three characters.
3. For each direction (`down`, `left`, `right`, `up`), provide idle 2 frames, walk 6 frames, and interact 4 frames. Mirroring is allowed only if clothing/hair asymmetry remains correct and is declared.
4. Use the same bottom-center foot anchor, body scale, and collision-footprint convention for every frame.
5. Deliver a JSON frame manifest with frame rectangles, animation IDs, order, duration/FPS, repeat behavior, anchor, and nominal collision footprint.
6. Do not bake checkerboards, shadows, labels, UI, or scenery into the sheets. A shadow may be a separate RGBA asset.
7. Add one complete sidecar per request and a MANIFEST fragment with SHA-256. Do not set runtime integration fields.

## Acceptance

- Alpha inspection reports non-interlaced 8-bit RGBA; decoded pixels include both actual transparency and visible character content. An opaque RGBA sheet with a baked checkerboard, or a fully transparent blank sheet, fails.
- Every declared animation contains distinct consecutive frames and loops without a size or foot-position jump.
- Characters remain identifiable when rendered 44-52 px tall and do not drift more than 1 px at the foot anchor.
- Keyboard and touch movement can display all four directions; interact animation completes and returns to idle.
- `check_materials.py` passes before any Phaser integration or runtime QA.

## Copyable generation brief

```text
Create three original production sprite packages for a bright, readable 2D narrative RPG: Kirito, Alice, and Eugeo as children in Rulid Village. Keep their canonical role readability but use an original visual treatment rather than copying anime frames or game sprites. Use one consistent frame cell size across all characters, preferably 64x96 pixels. For each character and each direction down/left/right/up, create idle 2 frames, walk 6 distinct frames, and interact 4 distinct frames. Deliver non-interlaced 8-bit RGBA sprite sheets whose decoded pixels contain both transparent background and visible character pixels; do not merely add an opaque alpha channel. Include no checkerboard, no background, no baked shadow, no text, and no scenery. Lock every frame to the same bottom-center foot anchor and consistent body scale. Characters must remain recognizable at 44-52 pixels tall. Include a JSON frame manifest listing frame rectangles, animation names, order, duration/FPS, loop behavior, anchor, and collision footprint, plus request-scoped sidecars with prompts, model/tool/version, source/rights information, and SHA-256 manifest rows. Do not submit one 1024x1024 pose per animation name.
```
